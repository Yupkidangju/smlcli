# Lessons Learned

`smlcli` 개발 및 유지보수 과정에서 얻은 주요 교훈과 경험을 기록합니다. AI의 자체 학습용 DNA 스토리지로 사용되며, 향후 아키텍처나 구조 변화 시 같은 실수를 반복하지 않기 위해 참고합니다.

## 기록 규칙
- 문제 상황과 근본 원인을 명확하게 정의합니다.
- 문제를 어떻게 해결했는지, 어떤 아키텍처적 Trade-off가 있었는지 기록합니다.
- AI 에이전트가 향후 유사한 코드를 짤 때 참고할 수 있도록 짧고 단호하게 기술합니다.

---

### [2026-04-15] AppState 동기화 및 보안 정책 강제(Enforcement) 교훈
- **상황:** 설정 마법사(Setup Wizard) 완료 후 저장 로직만 수행하고 `AppState` 내 메모리 값을 즉시 갱신하지 않아, 앱 재부팅 전까지는 설정을 인식하지 못하고 기본 "dummy_key"가 사용되던 치명적 기능 결함 발견.
- **해결/결정:** `Saving` 단계 완료 시 `save_config` 이후 즉시 `self.state.settings = Some(settings)`를 호출하여 런타임 상태를 핫-로드(Hot-load)하도록 수정. 또한, `PermissionEngine`을 통해 도구 실행 전 정책(ShellPolicy, FileWritePolicy 등)을 강제로 확인하는 단일 진입점(Checkgate)을 구축.
- **교훈:** 영구 저장소(Keyring, File)와 메모리 상태(State) 사이의 동기화 누락은 보안 및 기능상의 치명적 위험을 초래합니다. "Save-and-Apply" 패턴을 항상 명시적으로 구현하고, 도구 실행 로직은 반드시 중앙 집중형 정책 엔진(`PermissionEngine`)을 거치도록 설계하십시오.

---

### [2026-04-16] keyring 의존성과 크로스플랫폼 호환성 교훈
- **상황:** `keyring` 크레이트의 `sync-secret-service` feature 미지정으로 mock store가 사용되어 API 키가 영속화되지 않는 치명적 버그(beta.13)가 발생. 수정 후에도 gnome-keyring 미설치 환경(WSL, Docker)에서 실행 불가.
- **해결/결정:** keyring 크레이트를 완전 제거하고, `~/.smlcli/` 디렉토리에 마스터키 파일(.master_key) + TOML 설정(config.toml)의 파일 기반 암호화로 전환. ChaCha20Poly1305 필드별 암호화 유지, chmod 600 적용.
- **교훈:** OS 의존적 보안 저장소(keyring, Credential Manager)는 개발 환경에서는 작동하더라도 배포 환경(CI, headless, 컨테이너)에서 반드시 실패합니다. 크로스플랫폼 CLI 도구는 파일 기반 솔루션 + 적절한 파일 권한이 훨씬 안정적입니다. 또한, `cargo audit` 경고가 있는 크레이트(serde_yml)를 도입할 때는 반드시 RUSTSEC 데이터베이스를 먼저 확인해야 합니다.

---

### [2026-04-16] AI 응답 렌더링과 UX 피드백 교훈
- **상황:** AI 도구 호출 시 원시 JSON 스키마가 타임라인에 그대로 노출되어 사용자 혼란 야기. AI 추론 시 아무런 시각적 피드백 없음. 슬래시 명령어 자동완성 없음.
- **해결/결정:** (1) 타임라인 렌더링에 `filter_tool_json()` 필터 추가, (2) `is_thinking` 상태 플래그로 추론 인디케이터, (3) `SlashMenuState`로 자동완성 팝업, (4) 시스템 프롬프트에 ~1K 토큰 페르소나 정의.
- **교훈:** LLM 기반 CLI 에이전트에서 "AI가 출력하는 모든 것이 사용자에게 보인다"는 사실을 항상 고려해야 합니다. 도구 호출 JSON, 디버그 텍스트 등 내부 프로토콜이 사용자 타임라인에 노출되지 않도록 렌더링 레이어에서 필터링해야 하며, 비동기 작업에는 반드시 로딩 인디케이터를 동반해야 합니다.

### 3. 이벤트 아키텍처는 처음부터 세분화해야 한다 (v0.1.0-beta.18 분석)
- **문제:** 초기 설계에서 `Action` enum을 `ToolFinished`/`ChatResponseOk` 등 완료 이벤트 중심으로 7종만 정의했습니다. 이 구조는 MVP에서는 충분했으나, 진행 표시(스피너, 스트리밍, 작업 카드)를 구현하려 하자 시작·진행·완료·에러를 구분할 수 없어 구조적 한계에 부딪혔습니다.
- **해결/결정:** Action을 14종으로 확장하고 (ChatStarted, ChatDelta, ToolQueued, ToolStarted, ToolOutputChunk, ToolSummaryReady 추가), `session.messages`(LLM 컨텍스트)와 `timeline`(UI 카드)를 분리하는 이중 데이터 모델을 도입합니다.
- **교훈:** 비동기 에이전트 시스템에서 이벤트 enum은 "무엇이 끝났는지"만이 아니라 "무엇이 시작되었고 어디까지 진행되었는지"까지 표현할 수 있어야 합니다. MVP 단계에서도 라이프사이클 전체(Queued → Started → Progress → Done/Error)를 설계해두면 이후 UI 확장 시 재작업을 피할 수 있습니다. 또한 LLM 컨텍스트와 사용자 화면 표시는 목적이 다르므로 반드시 분리해야 합니다.

---

### [2026-05-22] 로컬 LLM 통합 및 오프라인 회복력(Fallback) 확보 교훈 (v3.8.0)
- **상황:** 로컬 LLM 런타임인 LM Studio 공식 프로바이더를 추가하면서 API Key가 없는 구조 및 TUI 위저드의 선형 흐름 편집 요구를 수용해야 했습니다. 특히 로컬 런타임 특성상 서버가 오프라인일 때 모델 핑(`/models`)이 실패하더라도 마법사 진입이 좌절되지 않고 수동으로 통과할 수 있는 유연한 에러 회복력이 결여되어 있었습니다.
- **해결/결정:** 위저드에 `BaseUrlInput` 분기를 추가하여 API Key 수집을 생략하도록 하고, static RwLock 전역 API를 도입해 런타임 도중 사용자 입력 URL이 즉시 반영되도록 구현했습니다. 또한, 통신 실패 여부와 관계없이 모델 목록에 `"✏ 직접 입력..."` 수동 지정 항목을 동적으로 생성하고, 선택 시 전용 입력 팝업(`is_custom_model_mode`)을 전개하여 로컬 모델명을 타이핑 주입해 저장이 가능하도록 복구 기작을 도입했습니다.
- **교훈:** 로컬/온프레미스 AI 시스템을 통합할 때는 클라우드 SaaS API와 다르게 네트워크 오프라인 또는 프로세스 비작동이 '정상적인 상태'의 일부임을 인지해야 합니다. API 응답 실패를 치명적 오류로 처리하여 사용자 흐름을 중단(Panic/Halt)시키지 말고, 항상 수동 Fallback 모드("✏ 직접 입력..." 등)와 로컬 런타임 갱신 주입구를 상시 개방하는 방어적이고 회복력 있는 UI/UX를 설계하십시오.

---

### [2026-05-22] 로컬 무인증 API 런타임 우회 정책 및 TUI 인덱스/테스트 정합성 교정 (v3.8.1)
- **상황:** v3.8.0에서 설정 마법사 내 LM Studio 온보딩 흐름을 추가했으나, 실제 챗 세션 구동 런타임(`chat_runtime.rs`)에서 API Key를 필수로 조회하려 시도하다가 누락 에러가 발생하거나 엉뚱한 폴백 프로바이더로 넘어가는 연동 누락을 확인했습니다. 또한 종합 대시보드(/config) 메뉴와 TUI 팝업 인덱스가 불일치하여 커서 조작 시 오작동이 있었고, 로컬 테스트 환경의 `/tmp/.git` 폴더 노이즈로 인해 일부 워크스페이스 fallback 통합 테스트가 실패하는 현상이 관측되었습니다.
- **해결/결정:** LmStudio 런타임 호출에 대해 `resolve_credentials` 및 `resolve_credentials_for_provider` 내에서 암호화 저장소 API 키 조회를 완벽하게 건너뛰고 빈 키(`""`)로 즉시 통과하도록 가드 정책을 적용했습니다. TUI 대시보드 팝업 영역을 `5 + custom`으로 상향하고 인덱스 분기 및 오프셋 보정(`saturating_sub(6)`)을 적용하여 커서 매핑을 완비했습니다. 또한, 상위 디렉토리에 뜻하지 않게 실존하는 `.git` 환경 노이즈를 격리 방어하기 위해 `test_find_workspace_root_fallback` 검증문을 조상 노드 실존 체크형 루프로 리팩토링했습니다.
- **교훈:** 
  1. **온보딩 설정과 실제 실행(Runtime)의 완벽한 얼라인먼트:** 마법사나 UI 단에서 자격을 생략하거나 Fallback을 지원하더라도, 정작 챗 세션을 구동하는 코어가 동일한 가드 정책을 따르지 않으면 무용지물이 됩니다. 런타임 크레덴셜 해소 분기에서도 반드시 동일한 예외 처리 모델을 유기적으로 결합하십시오.
  2. **TUI 인덱스 상수 및 오프셋의 중앙 통제:** TUI 내에서 리스트 렌더링 영역이 동적으로 늘어나거나 상수가 하드코딩되어 있을 때, 메뉴 인덱스 파싱과의 1:1 대응이 깨지기 쉽습니다. 새로운 프로바이더를 추가할 때는 네비게이션 최대 한계 한계값, 팝업 선택기, 커서 매핑 오프셋까지 동시 갱신되어야 함을 항시 유념하십시오.
  3. **환경 노이즈로부터 무결한 독립형 테스트 설계:** 테스트 환경은 사용자의 로컬 머신이나 CI/CD 샌드박스 상태에 따라 `/tmp/.git` 등 예상치 못한 데이터 오염이 실재할 수 있습니다. 상위 파일 경로를 거슬러 올라가는 탐색적 유틸리티 테스트의 경우, 환경 노이즈가 기댓값에 유입되어 단언(assertion)을 깨뜨리지 않도록 방어적인 조상 실존 루프로 견고하게 설계하여 빌드 신뢰성을 담보해야 합니다.

---

### [2026-05-23] TUI Modernization & Responsive Multi-viewport Redesign 교훈 (v3.9.0)
- **상황:** 기존의 TUI 디자인은 일반 웹 페이지 레이아웃 스펙 기반으로 고안되어 터미널 TUI 환경의 시각적 한계(단색, 색상 체계 부족, 개행 너비 충돌, 깜빡임 잔상, 대규모 렌더링 성능 저하 등)를 극복하기 힘든 제약이 있었습니다. 특히 5000라인 이상의 대용량 Diff 파싱 시 매 프레임 파싱하는 CPU 오버헤드로 인한 프리징 현상, 터미널 100 cols 미만 시 3분할 뷰포트의 오버플로우 깨짐, 설문조사 모달의 잔상 및 엇갈린 가로세로 배치 등이 문제였습니다.
- **해결/결정:**
  1. **고스케일 팔레트 및 다국어 5대어 실연결:** 11대 RGB 고스케일 세맨틱 팔레트 명세를 설계하여 하드코딩 색상을 100% 제거하고, 다국어 5대어 `I18nManager`를 실연결해 UI 정합성을 확보했습니다.
  2. **반응형 3분할 및 Tree of Thoughts 트리 드로잉:** `Borders::LEFT` 2px 세로바(`┃`) 기호 및 Tree of Thoughts 깊이 들여쓰기 융합 브랜치(`└─ ⚙️`) 트리 드로잉 개편으로 unicode-width 기반의 풀위드 텍스트 래핑 처리를 완비했습니다.
  3. **Diff 라인 캐시 가속 및 단축키 바인딩:** `DIFF_RENDER_CACHE` thread_local 기반 5000라인 가속 디프 라인 캐싱 기법을 적용하여 CPU 프리징을 완벽하게 예방했고, `Alt+1`~`Alt+6` 단축키 바인딩 인스펙터 탭바 연동으로 인터랙션 효율을 향상시켰습니다.
  4. **Composer Inset & Fuzzy Command Palette 커서 연동:** Composer Inset과 Fuzzy Command Palette에 각각 `bg_lowest`, `bg_panel` 단색 배경 및 `Clear` 위젯 소거 처리를 이식하고, `tick_count`를 활용한 500ms 깜빡임 `█` 커서 점멸 연동을 구현했습니다.
  5. **설문조사 모달 수학적 정렬:** 설문조사 플로팅 모달을 수학적 중앙 정렬 비율(가로 60%, 세로 45%) 공식으로 보정하고, `Clear` 위젯 잔상 소거 및 다국어 tr() 매핑 연동을 완료했습니다.
  6. **Clippy 경고 해결:** clippy 경고 4건을 thread_local const 이니셜라이저 및 `#[allow(clippy::collapsible_if)]` 속성 부여로 완전히 제거하여 빌드 무결성을 확보했습니다.
- **교훈:**
  1. **TUI 렌더링 성능 최적화의 필수성:** TUI는 웹과 달리 색상 영역이 좁고 성능이 매 프레임 그리기에 의존하므로, 중복된 문자열 파싱이나 무거운 CPU 계산(예: Diff 파싱)은 반드시 thread_local 등 캐시 기법으로 격리해야 극적인 성능 향상을 도출할 수 있습니다.
  2. **수학적 정렬 및 반응형 레이아웃의 유연함:** 터미널 해상도는 동적으로 변화하므로, 고정 크기가 아닌 비율 기반 수학적 중앙 정렬 공식 및 터미널 너비 기반 반응형 탭 드로잉을 구축해야 오버플로우나 잔상이 없는 견고하고 프리미엄한 UI를 보장할 수 있습니다.
  3. **Clippy 경고와 컴파일러 사양 호환성:** Clippy가 제안하는 nested `if` collapsible_if 수정안이 stable/unstable let_chains 사양에 영향받아 빌드를 깨뜨릴 수 있으므로, 억지로 collapsible_if를 축소하기보다 `#[allow(clippy::collapsible_if)]`를 사용하는 것이 훨씬 더 안정적이고 크로스플랫폼 이식성이 높은 아키텍처적 결정이 됩니다.
